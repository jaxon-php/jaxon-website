---
title: Pagination
menu: Pagination
template: example
example:
    summary:
        - This example shows how to paginate an ajax request.
---

The `paginator()` method of the Jaxon functional component is used to paginate an ajax request to a component method.
The `page()` method receives as parameter a callback which displays the paginated content in the DOM element with a given id.
The `render()` method displays the pagination links in the DOM element with a given id.

---
title: Bind a component
menu: Bind a component
template: example
example:
    summary:
        - This example shows how to dynamically bind a component to a DOM node.
---

The `hello-text` id is used to bind the DOM element to the UI component, which is then used to set the DOM element content.

---
title: Jaxon HTML attributes
menu: HTML attributes
template: example
example:
    summary:
        - This example shows how to use Jaxon HTML custom attributes to attach a component to a DOM node, and define event handlers.
---

The code uses some features of the HTML helper returned by the `attr()` call.
- `bind()`: bind a UI component to a DOM element.
- `on()`: define an event handler on a DOM element.
- `html()`: insert the UI component's HTML content in a template.

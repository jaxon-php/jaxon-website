---
title: Le composant de pagination
menu: Le composant de pagination
template: example
example:
    summary:
        - This example demonstrates the built-in pagination component.
---

In addition to the user component, a pagination component is provided by the library.
The later is bound to a DOM element using the `attr()->pagination()` call.

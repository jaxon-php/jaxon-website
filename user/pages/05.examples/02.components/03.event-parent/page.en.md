---
title: Children event handlers
menu: Children event handlers
template: example
example:
    summary:
        - This example shows how to use Jaxon HTML custom attributes to define event handlers on child DOM nodes.
---

This example shows how to set event handlers on child nodes using a jQuery-like selector, defined with the `select()` and `on()` methods.

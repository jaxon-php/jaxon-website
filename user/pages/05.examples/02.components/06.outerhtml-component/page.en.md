---
title: Attributes in outerHTML
menu: Attributes in outerHTML
template: example
example:
    summary:
        - This example shows how to dynamically bind a component to a DOM node by setting its outerHTML property.
---

The UI component root element is changed, by setting its `outerHTML` attribute.

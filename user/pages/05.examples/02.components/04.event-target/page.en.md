---
title: Multiple event handlers
menu: Multiple event handlers
template: example
example:
    summary:
        - This example shows how to use Jaxon HTML custom attributes to define multiple event handlers on a single DOM node.
---

This example shows how to set multiple event handlers on child nodes using jQuery-like selectors, by chaining calls to the `select()` and `on()` methods.
